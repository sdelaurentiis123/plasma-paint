# First frozen-model / conditional SFT run

- NERSC Slurm job: `57924676`.
- Submitted: 2026-09-04.
- Source revision: `3ea45e3` (immutable bundle checkout).
- Directory: `/pscratch/sd/s/sod2112/plasma-paint-runs/20260904-3ea45e3/work`.
- Account: `m4810_g`; shared GPU queue, one GPU, 32 CPU cores, two-hour hard limit.
- Submission: `sbatch --account=m4810_g --cpus-per-task=32 scripts/nersc_baseline_sft.slurm`.
- Initial 16-core submission was rejected before allocation; the explicit 32-core override fixes the scheduler requirement.
- Public base weights: `Qwen/Qwen2.5-Coder-7B-Instruct`, downloaded inside this run directory.
- Environment: NERSC `pytorch/2.8.0` plus a project-local virtual environment with PEFT 0.17.1 and Node 24.19.0. Exact installed packages are recorded in the run's `artifacts/plasma_painter/dependencies-nersc.txt`.
- Inputs: existing permitted old-85604 feature cache; source manifests and normalization copied unchanged, feature paths rebased inside the isolated checkout.
- Sampling: 12 baseline-prompt and 12 optimized-prompt programs, 1800-token completion cap, deterministic per-candidate seeds.
- Training: conditional 40-step watercolor SFT LoRA only after the existing actual-model candidate count and 80% validity gate passes. No DPO or GRPO model update is included.
- Preflight: reference JavaScript painter passed the NERSC sandbox on two training frames, 196 operations per frame.
- No shared environment, dataset, unrelated project, permission, or other user's job was modified.

Submission is not evidence of completion. Read the job log and generated sample,
filter, and SFT manifests before claiming an adapter exists. This first run still
uses the old archive; the larger fit_profile corpus has not been substituted.
