# Rusty migration and creative studies

- Replacement job: `6984999`, source revision `dcd634e`.
- Dedicated directory: `/mnt/home/sdelaurentiis/ceph/plasma-paint-runs/20260904-rusty/work`.
- Queue: `gpupreempt`, account `cca`, QoS `gpupreempt`.
- Limit: one RTX Pro 6000 Blackwell GPU, eight CPUs, 64 GB host memory, two hours.
- NERSC job `57924676` was confirmed canceled with zero elapsed compute before the Rusty hold was released.
- Rusty was observed RUNNING on `workergpu175` after release.
- Python 3.11.11, Torch 2.8.0+cu128, Transformers 4.56.2, PEFT 0.17.1, Node 24.19.0; full freeze in `artifacts/plasma_painter/dependencies-rusty.txt` inside the run.
- Reference painter preflight passed on two permitted training frames: 196 operations per frame.
- Generation now saves each completed program before requesting the next, preserving completed work if preempted. This is not automatic resumption or an SFT optimizer checkpoint.
- The original 24-program and conditional 40-step SFT plan is unchanged. No preference optimization or online LM RL has been run by this submission.

The local color studio now includes a pencil study. Its contour paths and
gradient hatching use density features; shaded filament marks use extracted
component centroids and areas. It remains an untrained browser style study.

The local `/slice-space.html` arranges eight actual field-aligned sections as
triangulated panels. Fan, corridor, and ribbon modes and camera motion change
display coordinates only. This is an exploded section atlas, not a reconstructed
volume, physical flythrough, isosurface, or interpolation between sections.
All scalar colors still come from permitted training frames 0–7 with each
section's own training-fitted normalization. Camera animation starts paused.
